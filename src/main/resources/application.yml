spring:
  application:
    name: iotfsdb
  main:
    banner-mode: off

server:
  compression:
    enabled: true
    mime-types: application/json
    min-response-size: 2048

iotfsdb:
  root: /tmp/iotfsdb
  read-only: true
  max-query-size: 1000
  max-idle-seconds: 60
