package org.huebert.iotfsdb.series;

public enum Aggregation {
    AVERAGE,
    COUNT,
    COUNT_DISTINCT,
    FIRST,
    LAST,
    MAXIMUM,
    MEDIAN,
    MINIMUM,
    MODE,
    SQUARE_SUM,
    SUM
}
