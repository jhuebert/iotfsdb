package org.huebert.iotfsdb.schema;

public enum Reducer {
    AVERAGE,
    COUNT,
    COUNT_DISTINCT,
    FIRST,
    LAST,
    MAXIMUM,
    MEDIAN,
    MINIMUM,
    MODE,
    MULTIPLY,
    SQUARE_SUM,
    SUM
}
